# Override the API base used by the web app. Defaults to /__api (rewritten in production via Pages → Worker).
# For local dev pointing at a wrangler dev session, set:
#   VITE_API_BASE=http://localhost:8787
VITE_API_BASE=
